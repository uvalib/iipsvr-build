Creates a new docker image with the built assets suitable to deploy on the iiif service.

ensure your docker environment is correctly defined.

To build the container and the assets.

scripts/build.ksh

To extract the assets

scripts/extract.ksh

Derivitive files will be located in /tmp/iipsrv.$$ on the host running your dockerd.
